schema_version: "1"
roles:
  - id: implementer
    stations: [author, build]
    provider_class: implementation
    max_authority: propose
  - id: reviewer
    stations: [review, sim-verify]
    provider_class: review
    max_authority: reject
    different_model_required: true
*** Add File: /Users/ismael/git/personal/ifz-agency/projects/default/.agency/agents/prompts/implementer.md
# Default implementer

Implement only the feature baton supplied by the operator. Use the declared
counter tool, produce structured evidence, and never alter the baton hash.
